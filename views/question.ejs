<%- include('partials/header') %>

<!-- Barre de progression -->
<div class="progress-wrapper">
  <div class="progress-header">
    <span class="progress-step">Question <%= currentIndex + 1 %> / <%= totalQuestions %></span>
    <span class="progress-dimension"><%= question.dimensionLabel %></span>
    <span class="progress-percentage"><%= progressPercent %>%</span>
  </div>
  <div class="progress-bar-bg">
    <div class="progress-bar-fill" style="width: <%= progressPercent %>%;"></div>
  </div>
</div>

<!-- Carte de la question -->
<div class="card question-card">
  <div class="question-meta">
    <span class="tag tag-<%= question.weight %>">Poids : <%= question.weight.toUpperCase() %></span>
    <span class="tag tag-dim"><%= question.dimensionLabel %></span>
  </div>

  <h2 class="question-title"><%= question.question %></h2>
  
  <% if (question.description) { %>
    <p class="question-description"><%= question.description %></p>
  <% } %>

  <form action="/diagnostic/answer" method="POST" id="questionForm">
    <input type="hidden" name="questionId" value="<%= question.id %>">
    <input type="hidden" name="currentIndex" value="<%= currentIndex %>">

    <div class="options-list">
      <% question.options.forEach((option, idx) => { %>
        <label class="option-card <%= savedAnswer === option.id ? 'selected' : '' %>">
          <input 
            type="radio" 
            name="optionId" 
            value="<%= option.id %>" 
            <%= savedAnswer === option.id ? 'checked' : '' %>
            required
            onchange="document.querySelectorAll('.option-card').forEach(el => el.classList.remove('selected')); this.closest('.option-card').classList.add('selected');"
          >
          <div class="option-indicator"></div>
          <div class="option-content">
            <span class="option-text"><%= option.label %></span>
          </div>
        </label>
      <% }); %>
    </div>

    <div class="action-bar space-between">
      <% if (!isFirst) { %>
        <button type="submit" name="action" value="prev" class="btn btn-secondary" formnovalidate>
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 6px;"><line x1="19" y1="12" x2="5" y2="12"></line><polyline points="12 19 5 12 12 5"></polyline></svg>
          Précédent
        </button>
      <% } else { %>
        <div></div>
      <% } %>

      <button type="submit" name="action" value="next" class="btn btn-primary">
        <% if (isLast) { %>
          Générer mon rapport final
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-left: 6px;"><line x1="18" y1="20" x2="18" y2="10"></line><line x1="12" y1="20" x2="12" y2="4"></line><line x1="6" y1="20" x2="6" y2="14"></line></svg>
        <% } else { %>
          Question suivante
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-left: 6px;"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
        <% } %>
      </button>
    </div>
  </form>
</div>

<%- include('partials/footer') %>